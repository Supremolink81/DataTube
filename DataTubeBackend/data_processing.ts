import * as d3 from "d3";

type FilterInfo = {
    variable: string,
    minValue: number,
    maxValue: number,
}

const filterData = (dataTable: d3.DSVRowArray, filters: Array<FilterInfo>) => {
    
    for (let dataFilter of filters) {



    }

}